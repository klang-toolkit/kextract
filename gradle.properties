# Configuration pour kextract - Phase 2 Étape 2.1.1
# Généré automatiquement pour l'environnement macOS local

# Chemin vers le JDK (Temurin 25.0.3 détecté via /usr/libexec/java_home)
jdk_home=/Users/chaos/Library/Java/JavaVirtualMachines/temurin-25.0.3/Contents/Home

# Chemin vers LLVM (installé via Homebrew)
llvm_home=/opt/homebrew/opt/llvm

# Note: Ces propriétés sont utilisées par build.gradle pour:
# - llvm_home: Accéder à libclang.dylib et aux headers clang
# - jdk_home: Exécuter javac, jmod, jlink avec la bonne version

# Version de clang détectée: 22 (dans ${llvm_home}/lib/clang/22/)
# Version de LLVM: Homebrew clang version 22.1.4
